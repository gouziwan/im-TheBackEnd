const key = 'BU7BZ-UMKCJ-L2QFV-KBGEV-MJ6PV-QEFWX'

const port = "3000"

const host = "localhost"

const socketPort = "5000"

module.exports = {
    key,
    port,
    host,
    socketPort
}

module.port = port
module.host = host
module.socketPort = socketPort